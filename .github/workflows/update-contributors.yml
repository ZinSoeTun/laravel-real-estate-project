name: Update Contributors in README

on:
  push:
    branches:
      - main

jobs:
  contrib:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: jwiegley/readme-contributors@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
